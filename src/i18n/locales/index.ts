import channels from "./en/channels.json";
import translation from "./en/translation.json";

const localeResources = {
      en: {
            translation: translation,
            channels: channels,
      },
};

export default localeResources;
